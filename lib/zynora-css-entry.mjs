/**
 * Placeholder for package resolution. Vite replaces this with the generated icon font CSS
 * when the `zynora()` plugin is registered.
 */
throw new Error(
    'Import "zynora/css" only works with the Zynora Vite plugin. Add zynora() to plugins in vite.config.'
);
